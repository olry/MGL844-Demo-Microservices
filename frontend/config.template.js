// Fichier généré au démarrage du conteneur par envsubst.
// Le port est lu depuis la variable d'environnement GATEWAY_PORT
// (définie dans le .env à la racine).
window.GATEWAY = "http://localhost:${GATEWAY_PORT}";
